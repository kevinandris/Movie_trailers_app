name: First Workflow
on:
  push:
    branches:
      - main
jobs:
  # lint:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Get code
  #       uses: actions/checkout@v3
  #     - name: Install dependencies
  #       run: npm ci
  #     - name: Lint
  #       run: npm run lint
  dependency:
    # needs: lint
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      # - name: Build website
      #   run: npm run build
  # test:
  #   needs: lint
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Get code
  #       uses: actions/checkout@v3
  #     - name: Install dependencies
  #       run: npm ci
  #     - name: Test code
  #       run: npm run test
  deploy:
    needs: dependency
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      # - name: Build code
      #   run: npm run build
      - name: Deploy code
        run: echo "Deploying..."
